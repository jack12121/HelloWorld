import React, {Component} from 'react';
const  home = () => {
    console.log('home');
    return (
        <div>
        <p>React recommends using inline styles. 
        When we want to set inline styles, we need to use camelCase syntax. React will also automatically append px after the number value on specific elements. The following example shows how to add myStyle inline to h1 element.</p>
        </div>

    )
}
export default home;
